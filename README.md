```txt
npm install
npm run db:apply
npm run dev
```

```txt
npm run deploy
```

```bash
npm run deploy
# 初回のみ以下のように質問される。新しくプロジェクトを作成する場合は「Create a new project」を選択する
The project you specified does not exist: "hono-todo". Would you like to create it?
❯ Create a new project
# ブランチを指定する
✔ Enter the production branch name: … main
```
